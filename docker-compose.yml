services:
  aionui:
    build: .
    image: ghcr.io/chen-ye/aion-ui-service:latest
    pull_policy: always
    container_name: aionui
    restart: unless-stopped
    ports:
      - "25808:25808"
    volumes:
      - aionui_data:/root/.config/AionUi

volumes:
  aionui_data:
